# Local tooling / editor state
.serena/
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# Test / coverage artifacts
.coverage
.pytest_cache/

# Runtime state — the plugin writes this only in the install target, never here
state/

# Internal-only notes (provenance map, scratch) — never published
local/

# Python build / packaging artifacts
build/
dist/
*.egg-info/
.eggs/
.venv/

