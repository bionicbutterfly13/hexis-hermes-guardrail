"""Hexis Hermes guardrails — PyPI installer for the Hermes Agent plugin.

The plugin itself lives in the sibling ``hexis`` package (shipped with this
distribution). This package provides the ``hexis-hermes-guardrails`` console
command that copies the plugin into a Hermes profile. See ``cli.py``.
"""

__version__ = "0.1.0"
